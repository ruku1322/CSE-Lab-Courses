#include<stdio.h>
int main()
{
  int distance, fuel_efficiency ;
  scanf("%d %d", &distance &fuel_efficiency);
  fuel _needed= (distance%fuel_efficiency);
  printf("%d %d", fuel_needed);
  return 0;
}
